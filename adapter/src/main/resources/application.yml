jwt:
  secret: ${JWT_SECRET}
  validity-duration: PT100S

external-login:
  facebook:
    graph-api-base: https://graph.facebook.com

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}